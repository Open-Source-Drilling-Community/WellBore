apiVersion: v1
kind: Service
metadata:
  name: {{ include "norcedrillingwellborewebappclient.fullname" . }}
  labels:
    {{- include "norcedrillingwellborewebappclient.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "norcedrillingwellborewebappclient.selectorLabels" . | nindent 4 }}
