apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "osdcdrillingwellboreservice.fullname" . }}-test-connection"
  labels:
    {{- include "osdcdrillingwellboreservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "osdcdrillingwellboreservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
