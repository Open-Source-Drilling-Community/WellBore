apiVersion: v1
kind: Service
metadata:
  name: {{ include "norcedrillingwellboreservice.fullname" . }}
  labels:
    {{- include "norcedrillingwellboreservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "norcedrillingwellboreservice.selectorLabels" . | nindent 4 }}
